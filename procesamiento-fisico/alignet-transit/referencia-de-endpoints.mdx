---
title: "Referencia de endpoints"
description: "Métodos, rutas y códigos HTTP de la interfaz local de Pay-me Transit PinPAD."
icon: "code"
---

El sistema central consume la interfaz HTTP/JSON que expone Pay-me PinPAD en el Wiseasy P5L. Construye todas las solicitudes a partir de la URL configurada para cada terminal:

```text
http://<IP_DEL_PINPAD>:<PUERTO>
```

El puerto predeterminado es `8080`. Confirma la IP y el puerto de cada terminal antes de integrar.

## Endpoints disponibles

| Método | Ruta | Uso | Respuestas documentadas |
| --- | --- | --- | --- |
| `GET` | `/health` | Verificar que Pay-me PinPAD esté accesible. | `200` |
| `POST` | `/authorize` | Iniciar un cobro síncrono. | `200`, `202`, `400`, `409`, `503` |
| `GET` | `/payments/{operationNumber}` | Recuperar el estado de una autorización. | `200`, `202`, `404` |
| `POST` | `/cancel` | Cancelar una autorización que continúa en curso. | `200`, `202`, `404`, `503` |
| `POST` | `/reversals` | Extornar una venta aprobada. | `200`, `202`, `400`, `404`, `409`, `502`, `503` |
| `GET` | `/reversals/{operationNumber}` | Recuperar el estado de un extorno cuando el resultado es incierto. | `200`, `202`, `404`, `502` |

<Note>
  `POST /cancel` y `POST /reversals` son operaciones distintas. Ambas usan el `operationNumber` de la autorización original y se consumen en el mismo host y puerto que `POST /authorize`.
</Note>

## Verificar disponibilidad

```http
GET /health
```

```bash
curl "http://<IP_DEL_PINPAD>:<PUERTO>/health"
```

Respuesta esperada:

```json
{
  "status": "UP",
  "service": "Alignet ECR"
}
```

Usa `GET /health` al configurar el terminal y antes de iniciar operaciones cuando necesites comprobar disponibilidad. Una respuesta satisfactoria confirma la aplicación local; no confirma por sí sola que el procesador aprobará pagos.

## Autorizar y consultar un pago

`POST /authorize` requiere un cuerpo JSON. Consulta [Solicitud de autorización](/procesamiento-fisico/alignet-transit/parametros-de-envio) para conocer los campos, las validaciones y los ejemplos.

`GET /payments/{operationNumber}` no lleva cuerpo. Usa exactamente la referencia enviada en la autorización:

```bash
curl -i "http://<IP_DEL_PINPAD>:<PUERTO>/payments/44992528"
```

## Cancelar una autorización activa

`POST /cancel` requiere el `operationNumber` de la autorización original que continúa en curso. HTTP `200` entrega el resultado final en la misma respuesta; no requiere un GET posterior. Consulta [Cancelación de una autorización en curso](/procesamiento-fisico/alignet-transit/cancelacion) para revisar el contrato y el manejo de `202`.

No existe un endpoint de consulta de cancelaciones confirmado en la documentación disponible.

## Extornar una venta aprobada

`POST /reversals` requiere el `operationNumber` de la venta aprobada. HTTP `200` entrega el resultado final en la misma respuesta. Consulta [Extorno de una venta aprobada](/procesamiento-fisico/alignet-transit/extorno) para revisar el contrato completo.

`GET /reversals/{operationNumber}` se reserva para recuperar un extorno después de `202`, timeout o pérdida de conexión. No consulta cancelaciones y no reemplaza la respuesta final del POST.

## Rutas fuera del contrato vigente

No uses `/payment`, `/api/v1/payments`, `/api/v1/payments/{operationNumber}` ni `DELETE /charges` para esta integración. Tampoco diseñes el cliente alrededor de una respuesta anidada en `result.state`, los códigos locales `P01–P21` o campos como `transactionID`: pertenecen a especificaciones anteriores o a otros productos.

<CardGroup cols={2}>
  <Card title="Solicitud de autorización" icon="file-export" href="/procesamiento-fisico/alignet-transit/parametros-de-envio">
    Implementa el payload de `POST /authorize`.
  </Card>
  <Card title="Respuestas y códigos" icon="list-check" href="/procesamiento-fisico/alignet-transit/respuesta-estados-y-codigos">
    Interpreta códigos HTTP y estados transaccionales.
  </Card>
</CardGroup>
